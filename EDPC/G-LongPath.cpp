#include <bits/stdc++.h>
using namespace std;

int lengthSerch();

int main() {
  int n, m;
  cin >> n >> m;

  vector<int> x(m), y(m);
  for(int i= 0; i < m; i++) {
    cin >> x[i] >> y[i];
  }

  /*
  N頂点M辺の有向グラフG
  頂点x[i]から頂点y[i]に辺が張られている
  Gは閉路を含まない
  */

}

